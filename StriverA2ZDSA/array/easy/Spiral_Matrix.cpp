#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main(){
    vector<vector<int>> matrix;
    int col = matrix[0].size();
    int row = matrix.size();
    vector<int> ans;

    for(int i=0;i<row;i++){
        for(int j=0;j<col;j++){

        }
    }
    return 0;
}