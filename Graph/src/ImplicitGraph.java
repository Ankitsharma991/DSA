public class ImplicitGraph {
    // Implicit graph gives a 2d array in it's question and we have to take that as
    // our graph and performs the operations
    // implicit means internally that means already formed graph
    // in 2d array for every cell located on i, j. There 4 movements are possible
    // left, right, top and down.
    /*
     * i, j -> top -> i-1, j;
     * i,j -> right -> i, j+1;
     * i,j -> down -> i+1, j;
     * i,j -> left -> i, j-1;
     */

    // Flood-fill algorithm is an algorithm where a 2d graph is given and implicit
    // Graph is used

}
