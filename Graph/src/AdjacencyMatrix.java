public class AdjacencyMatrix {
    // <----- Unweighted graph ----->
    // using AdjacencyMatrix Method a graph can be created by implementing a 2d
    // matrix where each corresponding vertex of value i, j will be filled with 1 if
    // they are neighbours of each-other otherwise value will be stored as 0.

    // <----- Weighted Graph ----->
    // in case of weighted graph, if two vertex are neighbours of each-other then
    // there the weight will be stored to corresponding i, j place else o will be
    // same for not being neighbours to each-other.
}
