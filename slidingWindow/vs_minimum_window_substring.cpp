#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s = "ADOBECODEBANC";
    string t = "ABC";

    int count = t.length();
    int size = s.length();

    int i = 0, j = 0;

    map<char, int> mp;

    for (auto it : t)
    {
        mp[it]++;
    }

    while (j < size)
    {
    }

    return 0;
}